{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build --workspace=packages/messaging-schema && npm run db:generate --workspace=apps/backend && npm run build --workspace=apps/backend && npm run build --workspace=apps/frontend"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=apps/backend"
  }
}
