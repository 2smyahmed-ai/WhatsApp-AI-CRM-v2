{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && cd apps/backend && npx prisma generate && cd ../.. && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=apps/backend"
  }
}
