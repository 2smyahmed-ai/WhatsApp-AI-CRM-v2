{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run --workspace=packages/messaging-schema build && npm run --workspace=apps/backend db:generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
